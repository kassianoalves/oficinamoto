# ============================================
# CONFIGURAÇÕES DO DJANGO - OFICINA MOTO
# ============================================

# DEBUG MODE (True para desenvolvimento, False para produção)
DEBUG=True

# SECRET KEY (Gere uma nova para produção!)
SECRET_KEY=django-insecure-sua-chave-secreta-aqui-MUDE-EM-PRODUCAO

# ALLOWED HOSTS (Em produção, especifique os hosts permitidos separados por vírgula)
# Em desenvolvimento, deixe vazio para detectar automaticamente IP local
# ALLOWED_HOSTS=seu-dominio.com,www.seu-dominio.com,seu-ip

# BANCO DE DADOS (Opcional - padrão é SQLite)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# EMAIL (Para recuperação de senha e notificações)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-app

# CORS ALLOWED ORIGINS (Origens permitidas para CORS)
# CORS_ALLOWED_ORIGINS=http://localhost:5174,http://seu-ip:5174

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Em desenvolvimento: O IP local é detectado AUTOMATICAMENTE
#    Não precisa configurar ALLOWED_HOSTS
#
# 2. Em produção: Defina ALLOWED_HOSTS e DEBUG=False
#
# 3. A detecção automática funciona mesmo em redes diferentes
#    (casa, escritório, celular, tablet, etc)
#
# 4. Para acessar de qualquer máquina:
#    http://seu-ip-local:8000
#    http://seu-hostname:8000
#    http://localhost:8000 (na mesma máquina)
